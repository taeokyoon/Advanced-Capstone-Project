# ── Core AI & Computer Vision ────────────────
opencv-python         # 웹캠 제어 및 이미지 처리
mediapipe             # AI 자세 추적 (M1 지원 버전)

# ── GUI & Image Processing ──────────────────
Pillow                # Tkinter 이미지 표시 및 변환

# ── System Tray & Background ────────────────
pystray               # 시스템 트레이 아이콘 관리

# ── Backend & Firebase ──────────────────────
firebase-admin        # 파이어베이스 실시간 DB 및 업로드
requests              # 인증 및 API 통신용

# ── OS Specific (Windows Only) ──────────────
# winotify            # 윈도우 알림 (Mac에서는 무시됨)